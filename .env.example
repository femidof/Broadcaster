## Broadcaster build-time feature flags (Vite)
## These are compiled into the frontend bundle at build time.
## Default behavior when unset: OFF.

# Reliability Suite (Connection Doctor, error-to-fix UI)
VITE_FEATURE_RELIABILITY_SUITE=0

# Analytics (bitrate sparklines + session summary)
VITE_FEATURE_ANALYTICS=0

